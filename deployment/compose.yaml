services:

  torrelay:
    image: wollomatic/torrelay:8da842c96ce419b2d926ae0e6146ca0e1ec26946
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    read_only: true
    mem_limit: 4G # adjust
    stop_grace_period: 2m
    volumes:
      - ./torrc:/etc/tor/torrc:ro
      - data:/var/lib/tor
    network_mode: host # check if you want this

volumes:
  data:
    name: torrelay
